<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="onClick"
            type="com.example.vcare_app.onclickinterface.OnConclusionClick" />

        <variable
            name="conclusion"
            type="com.example.vcare_app.api.api_model.response.ConclusionResponse" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="6dp"
        android:onClick="@{()->onClick.onConclusionClick(conclusion)}"
        app:cardCornerRadius="12dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="6dp">

            <TextView
                android:layout_width="220dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="4dp"
                android:text="@{conclusion.title}"
                android:textColor="@color/strong_green"
                android:textSize="@dimen/text_small_title_size"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <ImageView
                getImage="@{conclusion.img}"
                android:layout_width="220dp"
                android:layout_height="220dp"
                android:scaleType="centerCrop"
                android:src="@drawable/default_avatar" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>